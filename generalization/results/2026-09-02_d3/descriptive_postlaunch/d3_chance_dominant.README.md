# D3 dominant-only confirmed-conditioned chance match (descriptive, post-launch)

Post-launch descriptive chance calibration conditions on the frozen P2 confirmed/scorable frame and uses 10,000 star-level derangements of the single dominant frequency; it accompanies, without replacing, the prespecified 100-permutation any-mode accidental-match audit and carries no inferential interval.

Ruling: generalization/reviews/G5prep/sol_round2.md, item 8 (F21, ADMIT-DESCRIPTIVE).
Fields on every row: analysis_status=postlaunch_descriptive, prespecified=false, interval=none.

conditioning=p2_frame_confirmed_finite_best_and_dominant: the exact frozen P2 scorable/usable/S_best=1 positives with best_status==confirmed and finite best and dominant frequencies. truth_basis=dominant_only_star_level_derangement: candidate frequencies and per-star tolerances (1.5/baseline_days) fixed; the single dominant frequency permuted at the star level; only fixed-point-free derangements accepted; exactly 10000 accepted derangements from PCG64(20260829); a hit is the frozen classifier returning exactly `direct` (`ambiguous` is not a hit); the denominator is every confirmed-conditioned frame member in every derangement. The q95 field is a quantile of the randomization distribution, not an interval.
