“The published replay bundle is byte-reproducible only in the attested production environment under the strict replay gate; analyses of the new campaign performed on macOS arm64 with Accelerate BLAS used a separately labeled decision-equivalent tier and are not claimed to be byte-for-byte reproductions of that bundle. Before campaign processing, this environment replayed the full prespecified 25-star gate roster and reproduced every categorical and selection decision, peak-grid index, and alias flag, with best-frequency values agreeing within \(10^{-12}\) relative; remaining last-bit differences arise from BLAS reduction order and fused multiply-add behavior. Across that replay, decision-bearing float64 discrepancies were approximately \(10^{-13}\) relative, raw float32 periodogram readbacks differed by at most \(5.2\times10^{-5}\), and A95 limits by at most \(2.0\times10^{-3}\); the latter two are reported quantities and enter neither campaign decisions nor estimands. The platform and software stack, decision-equivalent attestation, and measured maxima are recorded in every run manifest and the results-bundle README, while the strict gate remains mandatory for any claim of byte reproducibility.”

**Verdict: APPROVE-WITH-CHANGES.** Use this explicit separation between byte reproducibility and decision equivalence. Also correct “≤\(5\times10^{-5}\)” to the documented \(5.2\times10^{-5}\), unless a new audited replay supports the tighter bound.
