RESOLVED — `frozen_api.py` is now covered by `v2_digest`, propagated through artifact/lock, resume scan, end-run check, metrics sidecars, and comparison verification. Import-graph audit found no uncovered project runtime source; no relaunch bypass found. Named tests correctly cover inclusion and drift refusal.

VERDICT: ADMIT