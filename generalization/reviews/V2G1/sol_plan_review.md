1. **BLOCKING — the four smoke stars are development data, not holdout data.** Three are flag1 and one is flag0; their behavior directly changed the veto/rule ([V2_PLAN.md:239](/Users/jackneo/Documents/vonhippel-base9/astro-wd/generalization/v2/V2_PLAN.md:239)). Reporting both versions without naming the primary is inadequate. Reclassify them as `dev_smoke`, remove them from the holdout runner list, regenerate hashes, and state: “Primary holdout analyses exclude the four development smoke stars; results including them are contaminated sensitivity analyses only.” Resulting primary D3 roster counts are flag1=299 and flag0=1,149.

2. **MAJOR — the odd-KIC set is only a tuning holdout.** Detector architecture was chosen from full-D3 summaries, including eventual holdout observations ([V2_PLAN.md:12](/Users/jackneo/Documents/vonhippel-base9/astro-wd/generalization/v2/V2_PLAN.md:12)). Add: “Because architecture was selected using summaries from the complete frozen D3 campaign, this split holds out v2 execution and constant selection, not architecture development; it is internal post-selection validation, not independent or confirmatory validation.” A genuinely untouched claim requires another dataset. Also state that no alternative partition was evaluated against frozen outcomes.

3. **BLOCKING — tuning `n_window_peaks` is not exact.** The plan says the rule is reapplied offline ([V2_PLAN.md:162](/Users/jackneo/Documents/vonhippel-base9/astro-wd/generalization/v2/V2_PLAN.md:162)), while the rescorer admits that candidate clustering and cross-pass partners remain those of the original N ([rescore_v2.py:7](/Users/jackneo/Documents/vonhippel-base9/astro-wd/scripts/v2/rescore_v2.py:7)). Required change: run the full dev lists for all nine `(trend_window_days, n_window_peaks)` pairs; rescore offline only the phase and amplitude-ratio choices. Implement a selector that asserts all 81 combinations and expected denominators, computes J, and writes the evidence artifact. Define ties as “first feasible maximizer in the candidate-set order printed in §3”; if none is feasible, retain the default and record `tuning_constraint_failure=true`.

4. **BLOCKING — “holdout once” is not enforced.** The runner accepts any subset of one half, arbitrary declared inline constants, and repeated `--allow-holdout` runs ([run_v2_ls.py:169](/Users/jackneo/Documents/vonhippel-base9/astro-wd/scripts/v2/run_v2_ls.py:169), [run_v2_ls.py:211](/Users/jackneo/Documents/vonhippel-base9/astro-wd/scripts/v2/run_v2_ls.py:211)). Add a registered-holdout mode that:

   - requires the exact holdout-list SHA and forbids `--limit`;
   - requires `V2_CONSTANTS_FROZEN.json`;
   - verifies its v2-code, split, preregistration-commit, and evidence digests;
   - creates one `HOLDOUT_LAUNCH_<dataset>.json` lock before computation and permits only exact resume of that run.

5. **BLOCKING — D3 endpoint denominators are wrong in code.** `compare_engines.py` intersects the two tables and silently drops absent IDs ([compare_engines.py:184](/Users/jackneo/Documents/vonhippel-base9/astro-wd/scripts/v2/compare_engines.py:184)). Consequently current P1/P3 would use 288/1,117 rather than the declared 302/1,150; after smoke exclusion they must use 299/1,149, with missing light curves coded as failures. Construct the frame by left-joining v2 onto the complete split roster and hard-fail on unexplained missing runner IDs.

6. **MAJOR — replace the P2 complete-case frame.** “Usable in both arms” conditions on detector-dependent availability ([compare_engines.py:141](/Users/jackneo/Documents/vonhippel-base9/astro-wd/scripts/v2/compare_engines.py:141)). Primary paired P2 should use the pre-existing frozen P2 frame—Mo-joined, `S_best=1`, frozen-usable—and count a v2 unavailable result as nonrecovery. After smoke exclusion, that frame currently has n=211. Report the both-arms-usable frame only as sensitivity, plus availability transitions.

7. **MAJOR — statistical methods are sound in principle but incompletely implemented.** Exact McNemar for D3 paired binary outcomes, paired star bootstrap, common-draw target bootstrap, and one-sided 95% CP are appropriate. Required additions:

   - Declare `B=2000`, seed `20260902`, two-sided McNemar, and an exact discordance-bound fallback for degenerate bootstrap results.
   - Implement both eligible and usable P4 variants; current code implements only eligible ([compare_engines.py:99](/Users/jackneo/Documents/vonhippel-base9/astro-wd/scripts/v2/compare_engines.py:99)).
   - Add the declared chance-match result.
   - Replace the 24-control rate with the target-clustered injected-versus-paired-control trigger and strict-recovery contrasts.
   - Call STRONG a “descriptive operational screen, not a hypothesis test or confirmatory decision.” The ≤2/500 rule is acceptable only on that basis: even 0/500 has CP \(U_{95}=0.00597\), so this half-sample cannot pass frozen P5’s 0.5% criterion.

8. **MAJOR — D2 is not window-disjoint and the control endpoint is presently broken.** The split has 139 template windows crossing halves, including 43/106 nominal-B windows and 72 null windows. Only 24 of the 67 controls referenced by odd-TIC B shards are run, leaving 93/129 B-control pairs without their control. Minimum-change fix: before constants freeze, run only the 500 dev nulls; defer even-TIC B/control outputs until after holdout; assign all 67 odd-TIC-referenced controls to holdout, producing 129 B + 67 controls + 500 nulls. Disclose the 72 repeated null windows as fixed-window, independent-noise validation.

9. **MAJOR — alignment can remove astrophysical variation.** Offsets use each OID’s entire, potentially differently sampled epoch distribution ([align.py:44](/Users/jackneo/Documents/vonhippel-base9/astro-wd/scripts/v2/align.py:44)). Current D3 has no range-disjoint eligible OID pairs, but five pairs share zero nights and 34 share fewer than five. Add fixed `min_shared_nights=5`; estimate offsets from shared-night per-OID weighted medians and leave insufficient-overlap OIDs unshifted. Report overlap counts and endpoint sensitivity. Also replace “keep the anchor zero point” with “are left unshifted.”

10. **MAJOR — high-pass leakage and veto exposure must be acknowledged and measured.** A 30-day median does not prevent residual 0.03–24 c/d variability from aliasing into 24–1440 c/d; the contrary claim is unsupported ([detrend.py:18](/Users/jackneo/Documents/vonhippel-base9/astro-wd/scripts/v2/detrend.py:18)). Add a low-frequency-only injection audit on dev windows and the paired-control recovery endpoint. Remove “science frequencies are untouched by construction” ([V2_PLAN.md:81](/Users/jackneo/Documents/vonhippel-base9/astro-wd/generalization/v2/V2_PLAN.md:81)): data-driven loci, local-window tests, stronger-peak mirrors, and cross-pass partners can veto them. Mandatory descriptive output: truth-frequency veto exposure by dataset/pass/band and component, including union exposure. The 0.4% estimate is reasonable only for typical \(T\approx2765\) d: exposure is approximately \(8\,tol=12/T\approx0.43\%\) per partner before overlaps, and is larger for shorter baselines and multiple partners.

11. **MINOR — the fixed coherence gate is defensible.** It is a deterministic classifier validated on holdout; an uncertainty-aware significance statistic is not mandatory and would introduce additional assumptions. Add: “Coherence is an operational gate, not a statistical test; recorded phase errors do not enter the decision.” Report failures stratified by phase uncertainty and amplitude S/N. No rule-code change is required ([multiband.py:92](/Users/jackneo/Documents/vonhippel-base9/astro-wd/scripts/v2/multiband.py:92)).

12. **MAJOR — provenance is acceptable in concept but not yet complete.** Omitting campaign code from the raw-detector binding is acceptable because the metrics bundle separately records its spec/campaign SHAs. However, machine, split SHA, half, stars-list SHA, and preregistration commit are not currently binding keys ([run_v2_ls.py:234](/Users/jackneo/Documents/vonhippel-base9/astro-wd/scripts/v2/run_v2_ls.py:234)); add and verify them in sidecars/metrics. Bind the comparison output to both metric-manifest SHAs, split SHA, comparison-script SHA, constants artifact, and smoke exclusions. The files are currently untracked, so “PRE-REGISTERED/frozen by the commit” is not yet true ([V2_PLAN.md:3](/Users/jackneo/Documents/vonhippel-base9/astro-wd/generalization/v2/V2_PLAN.md:3)). Commit the revised plan, code, tests, and split before any further development run.

13. **MINOR — spot-check result.** The constants table matches `TUNABLE`; fixed-locus families, decision rule, split counts, and quoted hashes match the implementation. Remaining inconsistencies: window-peak de-duplication uses per-series baseline rather than the declared combined T ([window.py:98](/Users/jackneo/Documents/vonhippel-base9/astro-wd/scripts/v2/window.py:98)), and `make_split.py` creates trend-window subsets falsely attributed to §5 even though §5 requires full dev reruns ([make_split.py:111](/Users/jackneo/Documents/vonhippel-base9/astro-wd/scripts/v2/make_split.py:111)). Use combined T and remove those subset files/comments.

Required abstract wording: “After full-cohort frozen-arm failure analysis, we fixed a digest-locked but not byte-replay-attested v2 detector, selected four prespecified constants on development data, and evaluated it once on an internal odd-ID holdout excluding four stars used during development; this is post-selection internal validation, not confirmatory external validation.”

VERDICT: REVISE — exclude/reclassify smoke stars; qualify the tuning-only holdout; make tuning exact and deterministic; enforce exact single holdout execution; repair D3/P2/P4/control frames; sequester shared D2 native windows; add alignment/leakage/veto audits; and complete provenance binding before freeze.