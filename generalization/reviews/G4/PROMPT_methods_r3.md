G4 ROUND 3 (methods / referee) — closure. Your round-2 verdict: generalization/reviews/G4/sol_methods_r2.md (APPROVE-WITH-CHANGES: MAJOR-1 D2 row-level intervals incl. nested contingency and sensitivity; MAJOR-2 pair usability on both sides, quiet subset on pair_usable, unresolved controls must fail/record not skip, scorer must re-run the W_g strata guard and the control-resolution guard; MAJOR-3 sidecar SHAs bound into inputs_sha256 + provenance_sha256 in completion.csv + test-log archival; MINOR stale text; MINOR git_dirty untracked). Applied in commit cb90b71 (51 tests on the Mac; D1 unchanged; the laptop is deliberately NOT pulled while the gen2 pilot runs — its pull, metrics, and archive with raw JSONs + sidecars + both-machine test logs + SHA256SUMS follow the pilot per RUNBOOK). Verify with file:line evidence and runtime probes: (MAJOR-1) strip_intervals on the D2 contingency JSON; sensitivity lo/hi NaN for D2 (main); (MAJOR-2) d2_paired_controls: b_usable/control_usable/pair_usable columns, summary over pair_usable, quiet subset = pair_usable & not_detected with n_pairs_not_run reported, a nominal-B row without a control id raises, a control that did not run is recorded as control_status="not_run" (never silently skipped) — tests test_paired_controls_pair_usability_and_unresolved_controls; truth_d2 re-runs check_wg_strata and requires the violation list to equal the generation record, and requires every nominal-B control id to resolve to exactly one control row with the same template_source_id, pool_index and template_wg_contrasts — test test_truth_d2_control_resolution_guard; (MAJOR-3) every .prov.json SHA enters inputs_sha256 in main; completion.csv now carries provenance_sha256 (run_generalization_ls.py) and metrics verifies it when present; RUNBOOK requires both-machine pytest logs + SHA256SUMS in every pilot record; (MINOR) the cluster-function comment; the runbook test count (51); git_tracked_dirty + git_untracked_count. The builder docstring wording is deliberately deferred (editing build_d2_shards.py changes the generation-code SHA and would orphan gen2) — state whether that deferral is acceptable. Then restate the remaining pre-full-run checklist. Report per item RESOLVED/PARTIALLY/UNRESOLVED, any NEW finding tagged BLOCKING/MAJOR/MINOR with the exact fix, then "## Verdict: APPROVE | APPROVE-WITH-CHANGES | REJECT". Do not edit files.
