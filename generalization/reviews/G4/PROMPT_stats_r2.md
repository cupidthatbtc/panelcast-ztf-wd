G4 ROUND 2 (statistics). Your round-1 verdict: generalization/reviews/G4/sol_stats.md (PROCEED-WITH-AMENDMENT-4 with a recommended amendment text and three metric-layer fixes: surface pseudoreplication, degenerate paired-bootstrap intervals, chance-match mismatch; plus descriptive labelling of sensitivity Wilson rows). Amendment 4 is implemented in commit b854e97 (pytest tests/ -q = 46 passing in .venv-gen; D1 regression 11/13, 9/13, 13/13). Verify each item against the CURRENT files with file:line evidence and runtime probes where the sandbox allows: (1) stratification: scripts/generalization/build_d2_shards.py load_pool computes W_g = Σ_nights max(n_zg,night − 1, 0) and match_templates sorts by (wg_contrasts, source_id) with unchanged round-half-even 10/50/90 picks; d2_truth_model.check_wg_strata (strict K0<K1<K2, production refuses), manifest column template_wg_contrasts, WG_SURFACE_EDGES = (15, 41, 84, 217) = the pool's 20/40/60/80th percentiles (the builder recomputes and refuses production on mismatch; pool quantiles recorded in the generation manifest) — NOTE: the methods reviewer proposed W_g (= your n_eff minus the number of multi-exposure nights); assess whether W_g is an acceptable realization of your recommendation; (2) primary P4 = recovery endpoint (confirmed AND best_candidate_matches_dominant == direct) in metrics_generalization.d2_cluster_bootstrap, detection-only = "trigger" secondary; flags prespecified_primary / confirmatory_decision (P5 sole decision) — trigger_rates for P5; (3) paired controls: d2_paired_controls scores each control's candidate against the partner target's dominant injected mode (2×2 counts, B-only/C-only/union, target-standardized paired differences and P(R_B=1,R_C=0) with common draws, quiet-control-conditioned secondary, reuse table d2_control_reuse.csv from the full manifest); (4) surfaces: d2_surfaces / _target_equal_cells (n_windows, n_targets, target-equal point, target-cluster bootstrap when >= 5 targets, counts only otherwise; axes W_g × amplitude, period × amplitude, amplitude); (5) chance-match: d2_chance_match — 10,000 frozen-seed target-level derangements, all K together, vectorized direct matching to the permuted target's dominant mode and to any mode, target-equal rates; (6) degenerate paired contrasts → interval "cp_discordance_bound" with ±U95 on the discordance probability (contrast rows also carry discordance_u95); (7) sensitivity.csv rows labelled descriptive with clamped bounds; D2 row-level intervals in completeness/contingency suppressed; (8) the spec text: generalization/METRICS_SPEC.md v4 header, estimand vocabulary, P4 definition, units/strata paragraph, complementarity, surfaces, guards, outputs; generalization/GENERALIZATION_PLAN.md Amendment 4 paragraph; ledger entry at the end of generalization/reviews/G2_FREEZE.md (SHAs must match current bytes). Tests: tests/test_d2_shards_contract.py (W_g strata, target-level surfaces, chance-match, paired controls, degenerate contrast, stars-file guard). Report per item RESOLVED/PARTIALLY/UNRESOLVED with evidence, any NEW finding tagged BLOCKING/MAJOR/MINOR with the exact fix, then "## Verdict: APPROVE | APPROVE-WITH-CHANGES | REJECT". Do not edit files.
