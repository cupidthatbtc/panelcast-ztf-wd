PRE-METRICS ADJUDICATION (stats/methods lens) — admissibility of ONE descriptive addition, decided BEFORE any D3 metrics run. Context: the D3 FULL campaign L-S run is executing (no metrics computed yet). A descriptive peek at the non-representative 150-star D3 pilot (recorded in the project ledger; raw per-pass statuses, unweighted) showed labeled negatives (dsct_flag0) with ~32% raw confirmed rate, and 30 of the 33 confirmed passes sit in the LOW pass at f < 4/d with a hard pileup at ~1.00 and ~2.00 cycles/day (examples 0.994–1.006, 2.000–2.015/d) — the DIURNAL ground-based systematic/alias family, which the frozen pipeline does NOT veto (its veto covers the SIDEREAL family only; see scripts/run_lomb_scargle.py). The Kepler field is dense (see generalization/data/d3/crossmatch_freeze/README.md). Frozen context: generalization/METRICS_SPEC.md — the negative-class trigger rate P3 is prespecified as an UPPER BOUND (negatives are non-constant by construction); fp_frequency_distribution.csv is the prespecified alias-veto audit that will display the frequency structure; Amendment 4 (ledger, generalization/reviews/G2_FREEZE.md) prohibits estimand-hierarchy changes; the spec's freeze rule says post-first-campaign-run changes void the prespecification. QUESTION: may we ADD, as a purely DESCRIPTIVE table (not an estimand, no hierarchy change, disclosed as pilot-informed and post-launch), a decomposition of the D3 negative-class trigger rate into within-diurnal-band vs outside-band components — and if yes, specify the EXACT frozen band definition (e.g. confirmed frequency f such that min_k |f − k·1.0 d⁻¹| ≤ tol for k ∈ {1,2,3}, plus f < f_min_floor?, with tol fixed a priori — justify the tolerance against the frozen grid and typical baselines ~2,700 d), where it lives (trigger_rates.csv extra rows vs a separate d3_trigger_decomposition.csv), and the exact disclosure sentence. If NO: state that the fp-distribution table alone is the correct treatment and why. Also state whether the same decomposition should apply to the census arm's negative trigger rate (census has no frequencies — presumably no). Output: a decision "ADMIT-AS-DESCRIPTIVE | REFUSE", the exact rule text and file/wording if admitted, and "## Verdict: ADMIT | REFUSE". Do not edit files.
