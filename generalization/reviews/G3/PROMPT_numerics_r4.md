G3 ROUND 4 (numerics/physics). Your round-3 verdict: generalization/reviews/G3/sol_numerics_r3.md (APPROVE-WITH-CHANGES: MAJOR-1 production must realize exactly the 33 cadence_alt shards; MAJOR-2 paired same-subset common-bootstrap contrast; MINOR wording). Verify against the CURRENT files (commit 2ad86a5; pytest tests/ -q = 40 passing in .venv-gen; the sandbox may block tmp files — then read the tests): (1) d2_truth_model.check_cadence_alt_schedule (set identity with the v3 mixed set, exactly 33 in production, subset otherwise) called in build_d2_shards.main AND metrics.truth_d2, plus the manifest-row check (one K=1 arm-B row per scheduled target); unit test test_cadence_alt_schedule_identity; (2) metrics.d2_cluster_bootstrap now returns (table, contrasts): for every non-nominal arm-B scenario a paired contrast vs nominal K=1 on the identical target set — one row per target per side asserted, ONE common draw matrix for both vectors and the difference, usable = usable on both sides, eligible keeps missing as failures on both sides — written to d2_scenario_contrasts.csv; contract test asserts membership, p_scenario/p_nominal_k1/diff, and the symmetric-missingness behavior; (3) wording: id layout `CD` in the plan, module docstring and test comment; core maximum 3,299; freeze test counts. Also, since a real-data dry run of the generation now exists (laptop: 3,102 shards, all 103 targets, 33 cadence_alt, 76 dropout, 20 redilution, all matches at tol_0.25), assess whether any numerics concern remains before the production generation is built. Report per item RESOLVED/PARTIALLY/UNRESOLVED with evidence, any NEW finding tagged BLOCKING/MAJOR/MINOR with the exact fix, then "## Verdict: APPROVE | APPROVE-WITH-CHANGES | REJECT". Do not edit files.
