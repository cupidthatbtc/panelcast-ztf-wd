G3 ROUND 3 (methods/schema/provenance). Your round-2 verdict: generalization/reviews/G3/sol_methods_r2.md (REJECT; partial 2/4/5/6/8 + 4 new findings). Every residual was acted on in commit 049f09e; verify against the CURRENT files with file:line evidence and runtime probes (pytest tests/ -q = 38 passing in .venv-gen; D1 regression 11/13, 9/13, 13/13 re-run after the patch). Map each residual: (2) sensitivity_table groups by scenario + dominant_dropped + cadence_code and emits, for every non-nominal scenario, the nominal K=1 rate recomputed on that scenario's exact matched target set (metrics_generalization.py, sensitivity_table); (4) production is a pure function of the arguments — d2_truth_model.production_reasons requires MANDATORY_PRODUCTION_ARMS (incl. the new cadence_alt), no limit, 1000 nulls, 928 pool, 103 input targets; truth_d2 re-derives the production flag from the recorded args, asserts the scheduled run matrix unconditionally via assert_counts(expected_counts) — never guarded by emptiness — and requires 103 input targets in production; foreign rejected ids refused; unit test test_production_requires_the_full_arm_matrix; (5) truth_d2 verifies every generation output SHA, reproduces the generation id from its recorded basis, and requires frozen-file identity and shard-determining-code identity (D2_GENERATION_CODE: build_d2_shards/d2_truth_model/frozen_api — deliberately NOT the whole campaign snapshot, so later metrics/runner fixes cannot orphan an already-run generation; the full snapshot is still recorded as campaign_sha256_snapshot) — assess whether that scoping is acceptable; (6) metrics requires completion.csv beside the run manifest, cross-checks source_count/failures/duplicates/ids ⊆ generation, re-derives the pilot flag from limit/stars_file/completion and refuses a mismatch, restricts a pilot's truth to the ids it ran, and checks each sidecar's passes/env_digest/frozen_digest/campaign_digest/generation_id/attestation/result/shard SHAs plus the completion row; (8) runner --limit help no longer calls itself a pilot; NEW-1 production arm matrix (above); NEW-2 primary D2 aggregates (completeness/contingency/surfaces/fp-frequency/chance-match) computed on nominal arm B only; NEW-3 `confirmatory` = membership (P4: nominal-B DETECTION rows, both denominators, non-pilot; P5: non-pilot with 1000 completed; outcome stays in acceptance_u95_leq_0.005); NEW-4 validate_manifest_frame enforces enumerations, per-arm defaults, scenario AND campaign id recomputed from the row's fields, shared by builder and metrics (tests test_manifest_semantics_are_enforced). Also review Amendment 3 (cadence_alt) for provenance/schema consistency: manifest fields cadence_code/cadence_s, final id digit, SPOC v3 report bound into the generation inputs, 33-target assertion in production, non-pooling. Report per item RESOLVED/PARTIALLY/UNRESOLVED with evidence, any NEW finding tagged BLOCKING/MAJOR/MINOR with the exact fix, then "## Verdict: APPROVE | APPROVE-WITH-CHANGES | REJECT". Do not edit files.
