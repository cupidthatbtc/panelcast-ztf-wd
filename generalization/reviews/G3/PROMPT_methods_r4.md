G3 ROUND 4 (methods/schema/provenance). Your round-3 verdict: generalization/reviews/G3/sol_methods_r3.md (REJECT: BLOCKING D3 run-universe binding; MAJOR symmetric missingness in matched rows; MAJOR D2 FP-frequency frame; MAJOR cadence_alt cardinality; MAJOR crossed sensitivity axes; MINOR layout wording). All acted on in commit 2ad86a5 (pytest tests/ -q = 40 passing in .venv-gen; D1 regression 11/13, 9/13, 13/13 re-run). Verify each against the CURRENT files with file:line evidence and runtime probes where the sandbox allows: (BLOCKING) run_generalization_ls.py records shard_index path + shard_index_sha256 in the run manifest; metrics_generalization.py main requires --shards-dir for d2/d3, takes --shard-index (default <shards-dir>/shard_index.txt), requires its SHA == run manifest shard_index_sha256, index ids == on-disk shard set, completion ids ⊆ index (subset ⇒ pilot), index == generation shard set for d2, d3 completion ids ⊆ truth ids, and every sidecar's shard SHA is verified unconditionally with the shard file required to exist; RUNBOOK metrics commands updated. (MAJOR symmetric) sensitivity_table now restricts both sides to targets usable on BOTH sides; d2_cluster_bootstrap emits paired common-draw contrasts (usable = both usable; eligible = missing kept as failure on both sides) to d2_scenario_contrasts.csv — see the contract test's symmetric-missingness assertions. (MAJOR FP) fp_frequency_distribution receives the full frame and retains arm/scenario; test_fp_frequency_audit_uses_null_and_control_arms. (MAJOR cadence) d2_truth_model.check_cadence_alt_schedule in builder and truth_d2 + manifest-row identity; test_cadence_alt_schedule_identity. (MAJOR axes) _row_problem enforces mutually exclusive sensitivity axes (nominal = none, others = exactly one), crowdsap in (0,1], match/template_status vocabularies; test_manifest_semantics_are_enforced includes a self-consistent-id crossed-axes row. (MINOR) layout `CD` wording. Also confirm the shared-validator + generation-id scoping remain coherent, and whether anything blocks building the production generation and running the stratified pilot on the attested laptop. Report per item RESOLVED/PARTIALLY/UNRESOLVED with evidence, any NEW finding tagged BLOCKING/MAJOR/MINOR with the exact fix, then "## Verdict: APPROVE | APPROVE-WITH-CHANGES | REJECT". Do not edit files.
