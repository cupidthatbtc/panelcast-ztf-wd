G3 code review (gate G3 of generalization/GENERALIZATION_PLAN.md). You are the METHODS/SCHEMA/PROVENANCE reviewer. Read, in this repo: scripts/generalization/build_d2_shards.py, scripts/generalization/d2_truth_model.py, scripts/generalization/frozen_api.py, scripts/generalization/run_generalization_ls.py (validate_attestation, --shard-index, .prov.json), scripts/generalization/metrics_generalization.py (truth_d2, d2_cluster_bootstrap), generalization/METRICS_SPEC.md (D2 contracts, P4/P5), the D2 section of generalization/GENERALIZATION_PLAN.md, and generalization/reviews/G2_FREEZE.md. Context: the laptop is the attested production machine (full-928 replay gate PASS 2026-08-29, report at generalization/attestation/laptop_replay_full_2026-08-29/replay_report.json); D2 shards will be built and run on that machine; the frozen scripts are byte-pinned in frozen_api.py. Review with file:line evidence: (1) does build_d2_shards.py emit exactly the schema the frozen analyze_star consumes (columns, dtypes, band names zg/zr, bjd_tdb precision — note: a 17-digit bjd written and re-parsed by pandas differs by 1 ulp on this platform, see generalization/env/CROSS_PLATFORM_REPLAY.md; are synthesized shards written with the same float formatting as the published exposure shards?), and the shard_index.txt / shard_manifest.csv / injected_modes.csv / rejected_modes.csv contracts that metrics_generalization.py reads; (2) resume/drift guards (--resume, stale-shard refusal, campaign_file_shas start/end) and whether a partially built shard set can be silently mixed; (3) the run matrix size and the pilot --limit path; (4) whether every quantity metrics needs for P4 (target/stratum clustering, eligible vs usable denominators, K_t=empty rule) and P5 (1,000 COMPLETED Gaussian nulls, CP at observed x) is carried in the manifests so the metrics program never has to reconstruct it; (5) provenance: are the D2 truth inputs (d2_targets.csv/d2_modes.csv SHAs, the SPOC verification report) bound into the shard manifest; (6) anything that would let a referee say the injection is conditional on the detection outcome (circularity) — e.g. template selection using post-hoc L-S results. Output: "## Verdict: APPROVE | APPROVE-WITH-CHANGES | REJECT" then numbered findings tagged BLOCKING/MAJOR/MINOR with file:line and the exact fix. Do not edit files.
