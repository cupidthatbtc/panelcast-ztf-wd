# Pipeline outputs and run artifacts (regenerated by panelcast / the scripts)
outputs/
*.log
data/raw/catalog_lc_cache/
data/processed/
data/splits/
data/features/
data/audit/
models/

# Local env / tooling
.pixi/
.venv/
__pycache__/
*.pyc
.DS_Store

# paper manuscript source: local provenance only, not redistributable
data/roster/_source_provenance/jestin2026_draft.tex

# generalization campaign
.venv-gen/
generalization/data/*/raw/
.venv-spoc/

# Mac-side literal-name symlink so laptop run manifests (Windows relative attestation path) resolve (RUNBOOK step 6)
outputs\\generalization\\replay_gate_full\\replay_report.json

# v2 codex subagent transcripts (prompts and reports are tracked; raw run logs are not)
generalization/v2/codex/*_run.log
generalization/v2/codex/*_last_message.md
generalization/reviews/*/*_last_message.md
